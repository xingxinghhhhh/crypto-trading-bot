# 项目节点完成进展与下一唯一节点请求（2026-08-10）

## 项目与硬边界
- 项目 crypto-trading-bot；当前仍是离线研究 + public-only OKX 证据链，策略全部 not_ready。
- 禁止 private API、API key、账户/余额/持仓接口、create_order、真实下单、Demo/Live；不得因进度或结果放宽 readiness。
- 主线是 future-only OKX 1h accumulation：unique closed execution intervals 当前 160 / 500，剩余 340；在 maturity 500 与经济 readiness 之前不得计算 turnover/cost/capacity/return/PnL、选 winner 或授权交易。
- epoch-2 唯一 governed capture window 是 [2026-08-16T10:00:00Z, 2026-08-16T11:00:00Z)，尚未到窗口；本轮没有联网。

## 仓库状态
- 分支 codex/quant-research-development，HEAD=9c086ab；工作树保留此前所有未提交研发改动，未 stage/commit/push，禁止 reset/clean/覆盖无关改动。
- 本轮新增/更新：
  - src/crypto_bot/market/prospective_capture_attempt_journal.py
  - config.prospective-capture-attempt-journal.example.yaml
  - tests/test_prospective_capture_attempt_journal.py
  - docs/phase_reports/PHASE_2_PROSPECTIVE_CAPTURE_ATTEMPT_JOURNAL_2026-08-10.md
  - src/crypto_bot/market/prospective_epoch_capture_admission.py（新增公共 ticket validator）
  - src/crypto_bot/cli.py、README.md、.github/workflows/ci.yml
- 仍存在此前的大量未提交源码/config/tests/docs；本轮没有删除它们。

## 已完成的本轮唯一节点
节点名：Prospective Epoch-2 Capture Attempt Journal & Acceptance Receipt Contract MVP。

目标已实现：在不联网、不执行 Aug16 capture 的情况下，复用并完整验证已冻结的 epoch-2 admission ticket，冻结未来 attempt 的不可变 append-only 语义和 first-validator-pass acceptance 规则，并生成 zero-attempt contract。

### 规则与范围
- 生命周期冻结为 attempt_started → transport_failed 或 response_received → validation_failed，或 response_received → validation_passed → accepted。
- attempt 严格编号 1..N；仅 transport/validation failure 可重试；第一个 validation_passed 立即成为唯一 accepted；accepted 后禁止任何 attempt。
- 禁止 response selection/cherry-picking、手工 version 选择、删除、重排、out-of-window、backfill、跨 epoch ticket reuse。
- 本轮不执行真实 request、不保存未来 response、不创建 snapshot/transition/gate/segment/ledger/readiness、不增加样本、不计算任何经济值、不改 readiness。

### 真实离线结果
- journal identity=c3eb67624e1797aaed7b9ef7ad97c17c358dfadcaba30743d0b760585466d2da。
- admission ticket=ecffe826d4dbae57584bc1f63edb1c05898fbab52fd6b0d7bbeb545022fa3df3。
- journal_status=awaiting_future_attempt；epoch=2；window=[2026-08-16T10:00:00Z,2026-08-16T11:00:00Z)。
- attempt_count=0；accepted_attempt_count=0；accepted_snapshot_identity=null；actual_attempt_rows=0；network_activity_performed=false。
- current_samples=160；remaining_samples=340；turnover/cost/capacity/return/PnL rows=0；economic/PnL/readiness authorization=false。
- 输出为 journal.csv、policy.csv、constraints.csv、JSON marker，sentinel row 不是伪造 attempt；CSV→JSON 原子顺序；同名同 bytes 幂等、同名异 bytes fail closed。

## 依赖 identity 与实现验证
- 新 journal 只接受 admission ticket 公共 validator 验证通过的 canonical marker，并重新验证 ticket 的 assembly=674116b95e03705b478f285012148f0143c2ef82befccda5627ce2ab901bcc79、accumulation policy=8f4a90632bcf73df04c7e6f44924924db682cdc8373ed04c1f726cb6ce353092、sample maturity=32cf37484013bf7ee5739016cf5ef54e935ee2ac4dfcb429266f24adc885a178、readiness=0f160960684cf5a4ed5e7636f59e863652a3c0f3087eadb3ed61091bb026f27b、segment chain=c6e3ccad96735923760931afc3b81e16068e880d64a97ec4d34d84908a681f05。
- 公共 ticket validator 检查 marker filename/identity、transitive assembly/snapshot/request policy、artifact hashes、pending_future_window/160/500/zero-economics 状态；journal validator 检查 journal identity、ticket binding、artifact bytes 和 sentinel row。
- 新 CLI：
  python -m crypto_bot.cli freeze-prospective-capture-attempt-journal --admission-ticket <ticket.json> --config config.prospective-capture-attempt-journal.example.yaml --output-dir reports/prospective-capture-attempt-journal

## 验证证据
- 新 journal + admission 目标测试：19 passed。
- source/live safety 测试：2 passed；合计受影响节点 targeted/safety：22 passed（0.29s）。
- Ruff 全仓通过。
- CI research/safety mypy 清单（加入新模块后 54 source files）：Success，无错误。
- 新 CLI 已用真实 ticket 离线运行成功，validator 成功重放生成的 journal；独立 replay output-dir 生成相同 identity、文件名、bytes、SHA-256。
- git diff --check 通过，仅 Windows 换行提示。
- 当前 pytest --collect-only：514 tests collected；全仓 pytest -q --cov 在 300 秒受控窗口内未结束且无失败输出，因此不能宣称本轮全量通过。该超时尚未定位为本轮代码回归；不通过缩小范围来冒充全量绿。昨日旧节点历史记录仍为 502 passed/85.01%，但不是本轮新鲜全量证据。

## 当前风险与下一节点约束
- Aug16 之前只能开发纯逻辑 receipt/append/replay contract，不能伪造市场数据或提前改变 epoch 状态。
- 必须继续 future-only、point-in-time membership、fail-closed、content-addressed、deterministic、atomic artifacts，保留 160→500 与所有旧 identities。
- 不得重复已经完成的 admission ticket 或 attempt journal contract；下一节点应建立在现有 journal 上并有独立验收闭环。
- 请只给一个新的、最小可独立验收的“唯一开发节点名 + 单句目标”，明确范围内/范围外、文件/config/schema/CLI、依赖 identity 校验、未来 receipt/append 规则、bias 防护、artifacts/atomic order、单元与离线验收、质量门、风险及 Codex 必须核实的假设。不要给候选列表，不要执行 Aug16 capture，不要增加样本/经济值/readiness/paper/live，不要 stage/commit/push。
